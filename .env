# VoltageEMS simplified development configuration
# ========================


# User configuration for container permissions
HOST_UID=503         # User ID for file ownership
HOST_GID=20         # Group ID for container access

# Note: Service ports are configured in docker-compose.yml via SERVICE_PORT
# For local development, set SERVICE_PORT environment variable if needed

# Logging configuration (supports module-level)
# Production: RUST_LOG=info
# Development: RUST_LOG=debug
# Debug specific service: RUST_LOG=info,comsrv=debug
# Debug specific module: RUST_LOG=info,comsrv::protocol=trace
RUST_LOG=info

# Redis connection
# For local development and testing (本地开发和测试环境)
REDIS_URL=redis://localhost:6379
# Note: Docker Compose services override this with redis://voltage-redis:6379
