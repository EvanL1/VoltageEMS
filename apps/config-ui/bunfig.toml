# Bun 配置文件
[install]
# 使用硬链接以节省磁盘空间
peer = "auto"
exact = true

[install.scopes]
# 配置私有包源（如果有）
"@voltage" = { token = "$NPM_TOKEN", url = "https://npm.voltage.internal/" }

[run]
# 自动加载 .env 文件
dotenv = true

[test]
# 测试配置
root = "./src"
coverage = true