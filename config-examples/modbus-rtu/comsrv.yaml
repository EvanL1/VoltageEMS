# ComSrv Modbus RTU 配置示例
# 串口通信配置

channels:
  - id: 2001
    protocol: "modbus_rtu"
    parameters:
      port: "/dev/ttyUSB0"      # Linux 串口
      # port: "COM3"            # Windows 串口
      baud_rate: 9600
      data_bits: 8
      stop_bits: 1
      parity: "none"            # none, even, odd