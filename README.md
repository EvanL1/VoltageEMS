# VoltageEMS - 工业物联网能源管理系统

高性能的微服务架构工业物联网能源管理系统，基于Rust构建，支持多种工业协议和实时数据处理。

## 🚀 核心特性

### 微服务架构
- **API Gateway** (8089) - 统一API网关，JWT认证和请求路由
- **comsrv** (3000) - 工业协议网关，支持Modbus、CAN、IEC60870等协议
- **modsrv** (8092) - 设备模型计算引擎，实时数据处理
- **hissrv** (8082) - 历史数据服务，时序数据管理
- **alarmsrv** (8080) - 告警管理服务，实时告警检测
- **rulesrv** (8080) - 规则引擎，自动化控制逻辑
- **netsrv** - 云端网关（开发中）

### 技术栈
- **后端**: Rust + Axum框架
- **数据存储**: Redis (实时数据) + InfluxDB (历史数据)
- **消息总线**: Redis Pub/Sub + Hash存储
- **数据同步**: Lua脚本高性能处理
- **前端**: Vue 3 + Tauri桌面应用

## 🏗️ 系统架构

### Redis数据架构 v3.2

```
┌─────────────────────────────────────────────────────────────┐
│                      Web Application                        │
│            Web UI | Mobile App | HMI/SCADA                  │
└─────────────────────┬───────────────────────────────────────┘
                      │
               ┌──────┴──────┐
               │ API Gateway │ (8089)
               └──────┬──────┘
                      │
┌─────────────────────┴───────────────────────────────────────┐
│                    Redis Message Bus                        │
│              Pub/Sub | Hash Storage | Streams               │
└──┬──────────┬────────┬─────────┬──────────┬──────────┬──────┘
   │          │        │         │          │          │
┌──┴───┐  ┌───┴──┐  ┌──┴───┐  ┌──┴───┐  ┌───┴────┐  ┌──┴──┐
│comsrv│  │modsrv│  │hissrv│  │netsrv│  │alarmsrv│  │rules│
│(3000)│  │(8092)│  │(8082)│  │ (N/A)│  │ (8080) │  │(8080)│
└──┬───┘  └──────┘  └──────┘  └──────┘  └────────┘  └─────┘
   │
┌──┴──────────────────────────────┐
│            Devices              │
│   Modbus | IEC60870 | CAN | ... │
└─────────────────────────────────┘
```

### Hash存储结构

```
# 实时数据存储
comsrv:{channelID}:m    # 遥测数据 (measurements)
comsrv:{channelID}:s    # 遥信数据 (signals)
comsrv:{channelID}:c    # 遥控数据 (controls)
comsrv:{channelID}:a    # 遥调数据 (adjustments)

# 模型计算结果
modsrv:{modelname}:measurement
modsrv:{modelname}:control

# 告警数据
alarm:{alarmID}

# 规则定义
rulesrv:rule:{ruleID}
```

**重要特性**：
- Hash结构提供O(1)访问性能
- 支持百万级点位
- 6位小数精度标准化
- 点位ID从1开始（不再使用10001等分段）

### Lua脚本数据处理

VoltageEMS使用Lua脚本进行特定的数据处理任务：

#### HisSrv数据聚合
```lua
-- services/hissrv/scripts/
└── archive_aggregator.lua  # 历史数据聚合脚本
    ├── 1分钟聚合：从实时数据计算avg/min/max
    └── 5分钟聚合：从1分钟数据二次聚合
```

#### 其他Lua脚本
```lua
-- scripts/
├── comsrv_sync.lua      # 数据同步示例
├── data_processor.lua   # 数据处理工具
└── rule_engine.lua      # 规则引擎示例
```

Lua脚本主要用于：
- HisSrv的时序数据聚合
- 高性能的批量数据处理
- Redis内部的原子性操作
- 减少服务间的网络开销

## 🚀 快速开始

### 环境要求
- Rust 1.88+
- Redis 7.0+ (推荐Redis 8)
- InfluxDB 2.x
- Docker (可选)

### 启动顺序

```bash
# 1. 启动Redis
docker run -d --name redis-dev -p 6379:6379 redis:7-alpine

# 2. 启动InfluxDB
docker run -d --name influxdb-dev -p 8086:8086 influxdb:2.7-alpine

# 3. 按顺序启动服务
cargo run -p comsrv      # 工业协议网关
cargo run -p modsrv      # 计算引擎
cargo run -p hissrv      # 历史数据服务
cargo run -p alarmsrv    # 告警服务
cargo run -p rulesrv     # 规则引擎
cargo run -p apigateway  # API网关
```

### 开发命令

```bash
# 检查编译（推荐）
cargo check --workspace

# 格式化代码
cargo fmt --all

# 运行clippy
cargo clippy --all-targets --all-features -- -D warnings

# 运行测试
cargo test --workspace

# 监控Redis数据
redis-cli monitor | grep comsrv

# 查看Hash数据
redis-cli hgetall "comsrv:1001:m"
```

## 📊 数据流

### 实时数据采集
```
设备 → comsrv → Redis Hash → Lua脚本处理 → modsrv/alarmsrv → 前端
```

### 控制命令下发
```
前端 → API Gateway → Redis Pub/Sub → comsrv → 设备
```

### 历史数据存储
```
Redis → hissrv → InfluxDB → 时序查询API
```

## 🔧 配置说明

### 服务配置
每个服务都有独立的配置文件：
```
services/{service}/config/default.yml
```

### 点位配置
使用CSV文件配置点位映射：
```
services/comsrv/config/{Protocol}_CH{ChannelID}/
├── measurement.csv    # 遥测点
├── signal.csv        # 遥信点
├── control.csv       # 遥控点
└── adjustment.csv    # 遥调点
```

### 环境变量
```bash
RUST_LOG=debug         # 日志级别
REDIS_URL=redis://localhost:6379
INFLUXDB_URL=http://localhost:8086
```

## 🛡️ 安全特性

- JWT Token认证
- Redis ACL权限隔离
- HTTPS/TLS支持
- 敏感信息环境变量管理

## 📈 性能优化

- Hash存储结构，O(1)访问
- Lua脚本批量处理
- 连接池复用
- 异步I/O处理
- 6位小数精度标准化

## 🤝 贡献指南

1. Fork项目
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启Pull Request

## 📄 许可证

本项目采用MIT许可证 - 查看 [LICENSE](LICENSE) 文件了解详情