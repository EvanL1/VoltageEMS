# ModSrv Service Configuration
# Override defaults only when needed
# Priority: Config File > Environment Variables > Code Defaults
# All values will use defaults - ConfigLoader now handles empty files gracefully