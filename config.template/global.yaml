# ============================================================================
# VoltageEMS Global Configuration
# ============================================================================
redis:
  url: "redis://127.0.0.1:6379"
  pool_size: 50
  timeout_ms: 5000
  enabled: true
api:
  host: "0.0.0.0"
logging:
  level: "info"
  dir: "logs"
  rotation:
    strategy: "daily"
    max_size_mb: 100
    max_files: 7
